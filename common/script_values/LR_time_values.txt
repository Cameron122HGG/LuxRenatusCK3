# Returns the display year as a positive integer
#adjusted_year_BCAD = {
 #   value = current_year
  #  subtract = 5000
#
 #   if = {
  #      limit = {  <= 0 }
   #     multiply = -1
    #    add = 1
    #}
#}

# Returns positive value if AD, zero or negative if BC
# Use: script_value > 0 triggers AD, <= 0 triggers BC
lux_renata_is_ad = {
    value = current_year
    subtract = 5000
}

calendar_setting = {
    value = 0  #Defaulted to zero until set in game - Gregorian
    if = {
        limit = { has_game_rule = alexander_calendar }
        add = 1
    }
    else_if = {
        limit = { has_game_rule = macedonian_calendar }
        add = 2
    }
    else_if = {
        limit = { has_game_rule = roman_calendar }
        add = 3
    }
    else_if = {
        limit = { has_game_rule = egyptian_calendar }
        add = 4
    }
}
alexander_era_dead = {
    value = 0
    if = { 
        limit = { character:MEGAS_ALEXANDROS = { is_alive = no } }
        add = 1
    }
}