#####################
# SACRAL GAUL POLICIES #
#####################

@gallic_policy_laws_cooldown_value = 10
@gallic_policy_cost_value = 500

gallic_policy_laws = {
	default = gallic_manor_reform_law
	cumulative = no
	flag = imperial_policy

	gallic_defense_mobilization_law = {
		flag = gallic_defense_mobilization_law

		can_keep = {
			realm_law_use_gallic_policy_trigger = yes
			trigger_if = {
				limit = { this != top_liege }
				liege = { has_realm_law = gallic_defense_mobilization_law }
			}
		}

		can_pass = {
			trigger_if = {
				limit = {
					top_liege = this
				}
				custom_tooltip = {
					text = gallic_policy_laws_cooldown_desc
					NOT = { has_variable = gallic_policy_laws_cooldown }
				}
				tgp_gaul_defense_mobilization_valid_trigger = yes
			}
		}

		on_pass = {
			## Set cooldown
			set_variable = {
				name = gallic_policy_laws_cooldown
				years = @gallic_policy_laws_cooldown_value
			}
			tgp_gallic_policy_event_effect = yes
		}

		modifier = {
			same_heritage_county_advantage_add = 5
			men_at_arms_maintenance = -0.15
			fort_level = 1
			hostile_raid_time = 0.15
		}
		flag = governors_may_recruit_gallic_horse_archers
		flag = cheaper_gallic_domicile_military_buildings

		pass_cost = {
			prestige = @gallic_policy_cost_value
		}

		ai_will_do = {
			value = 250
		}
	}

	gallic_disarmament_law = {
		flag = gallic_disarmament_law
		flag = vassal_internal_wars_cost_doubled
		flag = gaul_administrative_can_build_castles

		can_keep = {
			realm_law_use_gallic_policy_trigger = yes
			trigger_if = {
				limit = { this != top_liege }
				liege = { has_realm_law = gallic_disarmament_law }
			}
		}
		
		can_pass = {
			trigger_if = {
				limit = {
					top_liege = this
				}
				custom_tooltip = {
					text = gallic_policy_laws_cooldown_desc
					NOT = { has_variable = gallic_policy_laws_cooldown }
				}
			}
		}

		on_pass = {
			## Set cooldown
			set_variable = {
				name = gallic_policy_laws_cooldown
				years = @gallic_policy_laws_cooldown_value
			}
			tgp_gallic_policy_event_effect = yes
		}

		modifier = {
			men_at_arms_recruitment_cost = 0.25
			gaul_feudal_government_vassal_opinion = -15
			development_growth_factor = 0.15
		}

		pass_cost = {
			prestige = @gallic_policy_cost_value
		}

		ai_will_do = {
			value = 200
			if = {
				limit = {
					ai_has_warlike_personality = yes
				}
				value = -50
			}
			if = {
				limit = { government_has_flag = government_is_gaul_feudal }
				add = -1000
			}
		}
	}

	gallic_isolation_law = {
		flag = gallic_isolation_law
		flag = better_physician_treatments

		can_keep = {
			realm_law_use_gallic_policy_trigger = yes
			trigger_if = {
				limit = { this != top_liege }
				liege = { has_realm_law = gallic_isolation_law }
			}
		}
		
		can_pass = {
			trigger_if = {
				limit = {
					top_liege = this
				}
				custom_tooltip = {
					text = gallic_policy_laws_cooldown_desc
					NOT = { has_variable = gallic_policy_laws_cooldown }
				}
			}
		}

		on_pass = {
			## Set cooldown
			set_variable = {
				name = gallic_policy_laws_cooldown
				years = @gallic_policy_laws_cooldown_value
			}
			tgp_gallic_policy_event_effect = yes
		}

		modifier = {
			development_growth_factor = -0.15
			epidemic_resistance = 15
			diplomatic_range_mult = -0.15
		}

		pass_cost = {
			prestige = @gallic_policy_cost_value
		}

		ai_will_do = {
			value = 0
			if = {
				limit = {
					top_liege = this
					any_realm_county = {
						any_county_province_epidemic = {
							count >= 1
							outbreak_intensity >= apocalyptic
						}
					}
				}
				add = 200
			}
		}
	}

	sacral_expansion_law = {
		flag = sacral_expansion_law
		flag = cheaper_gallic_domicile_military_buildings

		can_keep = {
			realm_law_use_gallic_policy_trigger = yes
			trigger_if = {
				limit = { this != top_liege }
				liege = { has_realm_law = sacral_expansion_law }
			}
		}

		can_pass = { 
			trigger_if = {
				limit = {
					top_liege = this
				}
				custom_tooltip = {
					text = gallic_policy_laws_cooldown_desc
					NOT = { has_variable = gallic_policy_laws_cooldown }
				}
				# Filtered with 'trigger_if' to make nicer tooltips
				trigger_if = {
					limit = {
						NOR = {
							has_realm_law = gallic_bureaucracy_2
							has_realm_law = gallic_bureaucracy_3
						}
					}
					has_realm_law = gallic_bureaucracy_2
				}
				trigger_if = {
					limit = {
						NOR = {
							has_realm_law = gallic_bureaucracy_2
							has_realm_law = gallic_bureaucracy_3
						}
					}
					has_realm_law = gallic_bureaucracy_3
				}
				is_at_war = no
			}
		}

		on_pass = {
			## Set cooldown
			set_variable = {
				name = gallic_policy_laws_cooldown
				years = @gallic_policy_laws_cooldown_value
			}
			tgp_gallic_policy_event_effect = yes
		}

		modifier = {
			mercenary_hire_cost_mult = -0.15
			men_at_arms_recruitment_cost = -0.15
			gaul_administrative_government_vassal_opinion = -35
			travel_danger = 20
		}

		pass_cost = {
			prestige = @gallic_policy_cost_value
		}

		ai_will_do = {
			value = 80
			if = {
				limit = {
					ai_has_warlike_personality = yes
				}
				add = 50
			}
			if = {
				limit = { government_has_flag = government_is_gaul_administrative }
				add = -1000
			}
		}
	}

	gallic_manor_reform_law = {
		flag = gallic_manor_reform_law

		can_keep = {
			realm_law_use_gallic_policy_trigger = yes
			trigger_if = {
				limit = { this != top_liege }
				liege = { has_realm_law = gallic_manor_reform_law }
			}
		}
		
		can_pass = {
			trigger_if = {
				limit = {
					top_liege = this
				}
				custom_tooltip = {
					text = gallic_policy_laws_cooldown_desc
					NOT = { has_variable = gallic_policy_laws_cooldown }
				}
			}
		}

		on_pass = {
			## Set cooldown
			set_variable = {
				name = gallic_policy_laws_cooldown
				years = @gallic_policy_laws_cooldown_value
			}
			tgp_gallic_policy_event_effect = yes
		}
		
		should_start_with = {
			realm_law_use_gallic_policy_trigger = yes
		}

		modifier = {
			domicile_build_gold_cost = 0.25
			domicile_monthly_influence_mult = -0.25
			domicile_build_speed = 0.25
			build_gold_cost = -0.15
			build_speed = -0.15
			gaul_administrative_government_vassal_opinion = -15
		}

		pass_cost = {
			prestige = @gallic_policy_cost_value
		}

		ai_will_do = {
			value = 100
			if = {
				limit = {
					ai_has_builder_or_pious_builder_personality = yes
				}
				add = 25
			}
			if = {
				limit = { government_has_flag = government_is_gaul_administrative }
				add = -100
			}
		}
	}

	gallic_bandit_crackdown_law = {
		flag = gallic_bandit_crackdown_law

		can_keep = {
			realm_law_use_gallic_policy_trigger = yes
			trigger_if = {
				limit = { this != top_liege }
				liege = { has_realm_law = gallic_bandit_crackdown_law }
			}
		}
		
		can_pass = {
			trigger_if = {
				limit = {
					top_liege = this
				}
				custom_tooltip = {
					text = gallic_policy_laws_cooldown_desc
					NOT = { has_variable = gallic_policy_laws_cooldown }
				}
			}
		}

		on_pass = {
			## Set cooldown
			set_variable = {
				name = gallic_policy_laws_cooldown
				years = @gallic_policy_laws_cooldown_value
			}
			tgp_gallic_policy_event_effect = yes
		}

		modifier = {
			city_holding_build_gold_cost = -0.1
			barons_and_minor_landholders_opinion = 15
			monthly_county_control_growth_factor = 0.05
			monthly_county_control_growth_add = 0.2
			development_growth_factor = -0.15
			travel_danger = -10
		}

		pass_cost = {
			prestige = @gallic_policy_cost_value
		}

		ai_will_do = {
			value = 0
			if = {
				limit = { capital_county.county_control <= low_county_control }
				add = 150
			}
		}
	}
}
