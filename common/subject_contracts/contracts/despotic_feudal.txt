despotic_feudal_tax_collector_obligations = {
	obligation_levels = {
		default = {
			levies_factor = {
				value = 0
				if = {
					limit = { exists = scope:tax_collector }
					add = {
						value =	{
							add = scope:tax_collector.tax_collector_aptitude:classical_tax_slot
							save_temporary_value_as = tax_collector_aptitude_value
							multiply = 0.05
						}
					}
				}
			}

			tax_factor = {
				value = 0
				if = {
					limit = { exists = scope:tax_collector }
					add = {
						value =	{
							add = scope:tax_collector.tax_collector_aptitude:classical_tax_slot
							save_temporary_value_as = tax_collector_aptitude_value
							multiply = 0.05
						}
					}
				}
			}
		}
	}
}
