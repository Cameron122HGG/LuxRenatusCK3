######################
##### DEV CHEATS #####
######################
eastern_roman_core_dev_decision_effect = {
	every_county_in_region = {
		region = custom_eastern_imperial_core
		county = { add_to_list = custom_eastern_imperial_core_list }
	}
	every_in_list = {
		list = custom_eastern_imperial_core_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

roman_italia_suburbicaria_dev_decision_effect = {
	every_county_in_region = {
		region = custom_roman_italia_suburbicaria
		county = { add_to_list = custom_roman_italia_suburbicaria_list }
	}
	every_in_list = {
		list = custom_roman_italia_suburbicaria_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

roman_italia_annonaria_dev_decision_effect = {
	every_county_in_region = {
		region = custom_roman_italia_annonaria
		county = { add_to_list = custom_roman_italia_annonaria_list }
	}
	every_in_list = {
		list = custom_roman_italia_annonaria_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

roman_hispania_dev_decision_effect = {
	every_county_in_region = {
		region = custom_roman_hispania
		county = { add_to_list = custom_roman_hispania_list }
	}
	every_in_list = {
		list = custom_roman_hispania_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

custom_roman_viennensis_dev_decision_effect = {
	every_county_in_region = {
		region = custom_roman_viennensis
		county = { add_to_list = custom_roman_viennensis_list }
	}
	every_in_list = {
		list = custom_roman_viennensis_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

custom_roman_lugdunensis_dev_decision_effect  = {
	every_county_in_region = {
		region = custom_roman_lugdunensis
		county = { add_to_list = custom_roman_lugdunensis_list }
	}
	every_in_list = {
		list = custom_roman_lugdunensis_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

custom_roman_britannia_dev_decision_effect = {
	every_county_in_region = {
		region = custom_roman_britannia
		county = { add_to_list = custom_roman_britannia_list }
	}
	every_in_list = {
		list = custom_roman_britannia_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

custom_roman_africa_dev_decision_effect = {
	every_county_in_region = {
		region = custom_roman_africa
		county = { add_to_list = custom_roman_africa_list }
	}
	every_in_list = {
		list = custom_roman_africa_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

custom_roman_syria_dev_decision_effect = {
	every_county_in_region = {
		region = custom_roman_syria
		county = { add_to_list = custom_roman_syria_list }
	}
	every_in_list = {
		list = custom_roman_syria_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

custom_roman_palestine_dev_decision_effect = {
	every_county_in_region = {
		region = custom_roman_palestine
		county = { add_to_list = custom_roman_palestine_list }
	}
	every_in_list = {
		list = custom_roman_palestine_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

custom_roman_aegyptus_dev_decision_effect = {
	every_county_in_region = {
		region = custom_roman_aegyptus
		county = { add_to_list = custom_roman_aegyptus_list }
	}
	every_in_list = {
		list = custom_roman_aegyptus_list
		limit = {
			holder = {
				NOT = { holder = root }
			} 
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}